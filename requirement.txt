
I have some partially implemented features, I need you to add appropriate UI.

The following is a list of server methods that have been implemented but need to have UI added. 

The database has some tables called command, menu, and menu_command. These are supposed to keep track of all possible commands from each of the 
player "menus". 

    Menu   Key	    Handler	 
1. Sector  U	handleUseTerraformDevice	
2. Sector  L	handleLandOnPlanet	
3. Planet  D	handlePlanetDisplay	
4. Planet  Z	handleDestroyPlanet
5. Planet  Q	handleLeavePlanet
6. Sector  G	handleListDeployedDrones


1. Use Terraform Device
It should print out the number of Terraform devices that are on the player's ship. 
If the player doesn't have any, a message to that effect is displayed and the user is returned to the Sector menu.  
If there are 1 or more, ask the user if they would like to use one in the current sector to create a planet. 
If they say yes, then choose a random planet type and create a planet of that type in the sector.
Once created, count the number of planets in the sector. If it's more than the max_planets_per_sector value form the current universe's edit (see table `edits`), 
then a new record should be inserted in the planet_collisions table. Pick one of the other planets in the sector at random to be one of the planets in the collision.
Pick a random hour between planet_collisions_min_hours and planet_collision_max_hours, and add that to the current time to calculate the collision time. 
Change the sector display (if necessary) to include information about any upcoming planet collision in the current sector.
Also make sure that the sector display is showing the list of planets in the sector.

2. Land on Planet
This command is partly implemented. 
